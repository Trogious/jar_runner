set i = 0
aws cloudformation delete-stack --stack-name jr$i ; @ i++ ; aws cloudformation create-stack --capabilities CAPABILITY_IAM --template-body file://JarRunnerStackCompiled.yaml --stack-name jr$i
